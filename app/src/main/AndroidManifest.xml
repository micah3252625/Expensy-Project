<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Expensy">
        <activity android:name="com.example.expensy.SiginActivity" android:theme="@style/Theme.Expensy.NoActionBar"></activity>
        <activity android:name="com.example.expensy.SignupActivity" android:theme="@style/Theme.Expensy.NoActionBar"></activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.Expensy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddIncomeActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Expensy" />
        <activity
            android:name=".AddExpenseActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Expensy" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Expensy.NoActionBar"></activity>
    </application>

</manifest>